import styled from "styled-components";

const StyledContact = styled.div`
  padding: 200px 0;
  background: #efefef;
`;

const StyledContactInfo = styled.div`
  padding: 10px;
  text-align: start;
`;

const StyledContactTitle = styled.div`
  display: inline-block;
  font-size: 16px;
  color: #d5a26e;
  border-bottom: 3px solid #d5a26e;
  margin: 20px 0 10px;
`;

const StyledContactText = styled.div`
  font-size: 16px;
`;

const StyledContactMap = styled.div`
  width: 100%;
`;

const StyledMap = styled.div`
  width: 100%;
  padding-top: 75%;
`;

export {
  StyledContact,
  StyledContactInfo,
  StyledContactMap,
  StyledContactTitle,
  StyledContactText,
  StyledMap,
};
